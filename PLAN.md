
# 📝 خطة عمل مشروع نظام ERP لتأجير السيارات

هذه الوثيقة هي خارطة الطريق لتطوير نظام ERP متكامل لإدارة عمليات تأجير السيارات، بناءً على مجموعة التقنيات الحديثة التي تم اختيارها.

---

## المرحلة 0: إعداد المشروع والأساسات (Project Setup & Foundation)

الهدف: تهيئة بيئة العمل، تثبيت المكتبات الأساسية، وبناء الهيكل الأولي للمشروع.

- [ ] **1. تهيئة المشروع:**
    - [ ] استخدام `Vite` لإنشاء مشروع React جديد.
    - [ ] الأمر: `npm create vite@latest erp-system -- --template react-ts`
- [ ] **2. تثبيت المكتبات الأساسية:**
    - [ ] تثبيت `tailwindcss` وتهيئته.
    - [ ] تثبيت `shadcn/ui` وإعداد المكونات الأساسية (مثل Button, Input).
    - [ ] تثبيت `react-router-dom` لإدارة المسارات.
- [ ] **3. ربط قاعدة البيانات (Supabase):**
    - [ ] إنشاء مشروع جديد على Supabase.

    - [ ] تثبيت مكتبة `supabase-js`.
    - [ ] إعداد متغيرات البيئة (Environment Variables) للاتصال بـ Supabase.
- [ ] **4. تثبيت مكتبات إدارة الحالة والبيانات:**
    - [ ] تثبيت `tanstack/react-query` و `zustand`.
    - [ ] تثبيت `react-hook-form` و `zod`.
- [ ] **5. بناء هيكل المجلدات (Folder Structure):**
    ```
    /src
    |-- /components  (المكونات المشتركة)
    |-- /features    (المجلدات الخاصة بكل ميزة، مثل Employees, Trips)
    |-- /hooks       (Hooks مخصصة)
    |-- /lib         (ملفات مساعدة، مثل supabase client)
    |-- /pages       (صفحات التطبيق الرئيسية)
    |-- /store       (ملفات Zustand)
    |-- App.tsx
    |-- main.tsx
    ```

---

## المرحلة 1: المصادقة والهيكل العام (Auth & Core Layout)

الهدف: بناء نظام تسجيل الدخول وإنشاء الواجهة الرئيسية للتطبيق.

- [ ] **1. نظام المصادقة (Authentication):**
    - [ ] تصميم جدول `profiles` في Supabase لتخزين معلومات المستخدمين الإضافية.
    - [ ] بناء صفحة تسجيل الدخول (Login Page).
    - [ ] استخدام Supabase Auth لإدارة جلسات المستخدمين.
    - [ ] إنشاء `AuthContext` أو Hook لإدارة حالة المستخدم.
- [ ] **2. بناء الواجهة الرئيسية (Dashboard Layout):**
    - [ ] تصميم مكون `Sidebar` بالقائمة التي حددتها.
    - [ ] تصميم مكون `Header` (لعرض اسم المستخدم، الإشعارات، زر الخروج).
    - [ ] إنشاء هيكل الصفحة الرئيسي الذي يجمع الـ Sidebar والـ Header مع مساحة المحتوى.
- [ ] **3. إعداد التوجيه (Routing):**
    - [ ] استخدام `react-router-dom` لتعريف مسارات التطبيق.
    - [ ] إنشاء مسارات محمية (Protected Routes) لا يمكن الوصول إليها إلا بعد تسجيل الدخول.

---

## المرحلة 2: الوحدات الأساسية (Core Modules - CRUD)

الهدف: بناء شاشات إدارة البيانات الأساسية (الموظفين، العملاء، الموردين).

- [ ] **1. إدارة الموظفين (Employees):**
    - [ ] تصميم جدول `employees` في Supabase.
    - [ ] بناء صفحة لعرض جميع الموظفين باستخدام `tanstack/react-table`.
    - [ ] إنشاء نموذج (Form) لإضافة وتعديل موظف باستخدام `react-hook-form` و `shadcn/ui`.
    - [ ] ربط النموذج بـ Supabase باستخدام `react-query` (useQuery, useMutation).
- [ ] **2. إدارة العملاء (Customers):**
    - [ ] تكرار نفس خطوات "إدارة الموظفين" لإنشاء CRUD للعملاء.
- [ ] **3. إدارة الموردين (Suppliers):**
    - [ ] تكرار نفس الخطوات لإنشاء CRUD للموردين.

---

## المرحلة 3: إدارة الرحلات والتقويم (Trips & Calendar)

الهدف: بناء جوهر النظام، وهو إدارة حجوزات ورحلات السيارات.

- [ ] **1. تصميم قاعدة البيانات:**
    - [ ] تصميم جدول `trips` وربطه مع `employees` و `customers`.
    - [ ] تصميم جدول `cars` لإدارة أسطول السيارات.
- [ ] **2. صفحة "جميع الرحلات":**
    - [ ] بناء جدول بيانات (`Data Table`) لعرض الرحلات مع دعم للفرز والفلترة حسب الحالة (مكتملة، قيد التنفيذ، ملغاة).
    - [ ] إنشاء نموذج حجز رحلة جديدة.
- [ ] **3. تقويم الحجوزات (Booking Calendar):**
    - [ ] تثبيت `fullcalendar` ومكتبات الربط مع React.
    - [ ] بناء صفحة تقويم تعرض جميع الحجوزات بشكل مرئي.
    - [ ] ربط التقويم ببيانات الرحلات من Supabase.
- [ ] **4. التعامل مع التواريخ:**
    - [ ] تثبيت `date-fns` واستخدامها لتنسيق التواريخ وحساب المدد الزمنية للرحلات.

---

## المرحلة 4: إدارة الموارد البشرية (HR Management)

الهدف: إضافة الوحدات الخاصة بإدارة شؤون الموظفين.

- [ ] **1. الحضور والانصراف:**
    - [ ] تصميم جدول لتسجيل الحضور.
    - [ ] بناء واجهة بسيطة للموظف لتسجيل الحضور.
- [ ] **2. الرواتب والمكافآت:**
    - [ ] تصميم الجداول الخاصة بالرواتب والمكافآت والخصومات.
    - [ ] بناء شاشات لإدارة هذه العمليات.

---

## المرحلة 5: الوحدات المالية والمستندات (Financials & Documents)

الهدف: إدارة الجانب المالي وإنشاء المستندات.

- [ ] **1. الفواتير وعروض الأسعار:**
    - [ ] تصميم جداول `invoices` و `quotes`.
    - [ ] بناء واجهة لإنشاء الفواتير بناءً على بيانات الرحلة.
- [ ] **2. تصدير البيانات:**
    - [ ] تثبيت مكتبة `xlsx`.
    - [ ] إضافة وظيفة لتصدير البيانات من جداول `tanstack/react-table` إلى ملفات Excel.

---

## المرحلة 6: لوحة القيادة والتقارير (Dashboard & Reporting)

الهدف: عرض ملخص شامل للبيانات الهامة.

- [ ] **1. بناء لوحة القيادة:**
    - [ ] تصميم واجهة تعرض إحصائيات سريعة (مثل: عدد الرحلات الحالية، إجمالي الدخل الشهري، السيارات المتاحة).
    - [ ] جلب البيانات وعرضها في بطاقات (Cards) ورسوم بيانية بسيطة.

---

## المرحلة 7: التحسين والنشر (Refinement & Deployment)

الهدف: التأكد من جودة التطبيق ونشره.

- [ ] **1. الاختبار والمراجعة:**
    - [ ] مراجعة شاملة لكل الميزات.
    - [ ] إصلاح الأخطاء وتحسين الأداء.
- [ ] **2. النشر (Deployment):**
    - [ ] اختيار منصة نشر (مثل Vercel, Netlify).
    - [ ] إعداد عملية النشر التلقائي من GitHub.
